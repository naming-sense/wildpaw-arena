// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { CombatEventType } from '../../wildpaw/protocol/combat-event-type';
import { SkillSlot } from '../../wildpaw/protocol/skill-slot';


export class CombatEventPayload {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):CombatEventPayload {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsCombatEventPayload(bb:flatbuffers.ByteBuffer, obj?:CombatEventPayload):CombatEventPayload {
  return (obj || new CombatEventPayload()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsCombatEventPayload(bb:flatbuffers.ByteBuffer, obj?:CombatEventPayload):CombatEventPayload {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new CombatEventPayload()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

eventType():CombatEventType {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : CombatEventType.ShotFired;
}

sourcePlayerId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

targetPlayerId():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

skillSlot():SkillSlot {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : SkillSlot.None;
}

damage():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

isCritical():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

serverTick():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

x():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

y():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

static startCombatEventPayload(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addEventType(builder:flatbuffers.Builder, eventType:CombatEventType) {
  builder.addFieldInt8(0, eventType, CombatEventType.ShotFired);
}

static addSourcePlayerId(builder:flatbuffers.Builder, sourcePlayerId:number) {
  builder.addFieldInt32(1, sourcePlayerId, 0);
}

static addTargetPlayerId(builder:flatbuffers.Builder, targetPlayerId:number) {
  builder.addFieldInt32(2, targetPlayerId, 0);
}

static addSkillSlot(builder:flatbuffers.Builder, skillSlot:SkillSlot) {
  builder.addFieldInt8(3, skillSlot, SkillSlot.None);
}

static addDamage(builder:flatbuffers.Builder, damage:number) {
  builder.addFieldInt16(4, damage, 0);
}

static addIsCritical(builder:flatbuffers.Builder, isCritical:boolean) {
  builder.addFieldInt8(5, +isCritical, +false);
}

static addServerTick(builder:flatbuffers.Builder, serverTick:number) {
  builder.addFieldInt32(6, serverTick, 0);
}

static addX(builder:flatbuffers.Builder, x:number) {
  builder.addFieldFloat32(7, x, 0.0);
}

static addY(builder:flatbuffers.Builder, y:number) {
  builder.addFieldFloat32(8, y, 0.0);
}

static endCombatEventPayload(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createCombatEventPayload(builder:flatbuffers.Builder, eventType:CombatEventType, sourcePlayerId:number, targetPlayerId:number, skillSlot:SkillSlot, damage:number, isCritical:boolean, serverTick:number, x:number, y:number):flatbuffers.Offset {
  CombatEventPayload.startCombatEventPayload(builder);
  CombatEventPayload.addEventType(builder, eventType);
  CombatEventPayload.addSourcePlayerId(builder, sourcePlayerId);
  CombatEventPayload.addTargetPlayerId(builder, targetPlayerId);
  CombatEventPayload.addSkillSlot(builder, skillSlot);
  CombatEventPayload.addDamage(builder, damage);
  CombatEventPayload.addIsCritical(builder, isCritical);
  CombatEventPayload.addServerTick(builder, serverTick);
  CombatEventPayload.addX(builder, x);
  CombatEventPayload.addY(builder, y);
  return CombatEventPayload.endCombatEventPayload(builder);
}
}
