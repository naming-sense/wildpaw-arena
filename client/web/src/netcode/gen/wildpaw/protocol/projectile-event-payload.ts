// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { ProjectilePhase } from '../../wildpaw/protocol/projectile-phase';


export class ProjectileEventPayload {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ProjectileEventPayload {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsProjectileEventPayload(bb:flatbuffers.ByteBuffer, obj?:ProjectileEventPayload):ProjectileEventPayload {
  return (obj || new ProjectileEventPayload()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsProjectileEventPayload(bb:flatbuffers.ByteBuffer, obj?:ProjectileEventPayload):ProjectileEventPayload {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ProjectileEventPayload()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

projectileId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

ownerPlayerId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

targetPlayerId():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

phase():ProjectilePhase {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : ProjectilePhase.Spawn;
}

serverTick():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

x():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

y():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

vx():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

vy():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

static startProjectileEventPayload(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addProjectileId(builder:flatbuffers.Builder, projectileId:number) {
  builder.addFieldInt32(0, projectileId, 0);
}

static addOwnerPlayerId(builder:flatbuffers.Builder, ownerPlayerId:number) {
  builder.addFieldInt32(1, ownerPlayerId, 0);
}

static addTargetPlayerId(builder:flatbuffers.Builder, targetPlayerId:number) {
  builder.addFieldInt32(2, targetPlayerId, 0);
}

static addPhase(builder:flatbuffers.Builder, phase:ProjectilePhase) {
  builder.addFieldInt8(3, phase, ProjectilePhase.Spawn);
}

static addServerTick(builder:flatbuffers.Builder, serverTick:number) {
  builder.addFieldInt32(4, serverTick, 0);
}

static addX(builder:flatbuffers.Builder, x:number) {
  builder.addFieldFloat32(5, x, 0.0);
}

static addY(builder:flatbuffers.Builder, y:number) {
  builder.addFieldFloat32(6, y, 0.0);
}

static addVx(builder:flatbuffers.Builder, vx:number) {
  builder.addFieldFloat32(7, vx, 0.0);
}

static addVy(builder:flatbuffers.Builder, vy:number) {
  builder.addFieldFloat32(8, vy, 0.0);
}

static endProjectileEventPayload(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createProjectileEventPayload(builder:flatbuffers.Builder, projectileId:number, ownerPlayerId:number, targetPlayerId:number, phase:ProjectilePhase, serverTick:number, x:number, y:number, vx:number, vy:number):flatbuffers.Offset {
  ProjectileEventPayload.startProjectileEventPayload(builder);
  ProjectileEventPayload.addProjectileId(builder, projectileId);
  ProjectileEventPayload.addOwnerPlayerId(builder, ownerPlayerId);
  ProjectileEventPayload.addTargetPlayerId(builder, targetPlayerId);
  ProjectileEventPayload.addPhase(builder, phase);
  ProjectileEventPayload.addServerTick(builder, serverTick);
  ProjectileEventPayload.addX(builder, x);
  ProjectileEventPayload.addY(builder, y);
  ProjectileEventPayload.addVx(builder, vx);
  ProjectileEventPayload.addVy(builder, vy);
  return ProjectileEventPayload.endProjectileEventPayload(builder);
}
}
