// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { StatusEffectKind } from '../../wildpaw/protocol/status-effect-kind';
import { StatusEffectPhase } from '../../wildpaw/protocol/status-effect-phase';


export class StatusEffectEventPayload {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):StatusEffectEventPayload {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsStatusEffectEventPayload(bb:flatbuffers.ByteBuffer, obj?:StatusEffectEventPayload):StatusEffectEventPayload {
  return (obj || new StatusEffectEventPayload()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsStatusEffectEventPayload(bb:flatbuffers.ByteBuffer, obj?:StatusEffectEventPayload):StatusEffectEventPayload {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new StatusEffectEventPayload()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

effectId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

sourcePlayerId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

targetPlayerId():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

kind():StatusEffectKind {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : StatusEffectKind.None;
}

phase():StatusEffectPhase {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : StatusEffectPhase.Apply;
}

durationTicks():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

magnitude():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

serverTick():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startStatusEffectEventPayload(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addEffectId(builder:flatbuffers.Builder, effectId:number) {
  builder.addFieldInt32(0, effectId, 0);
}

static addSourcePlayerId(builder:flatbuffers.Builder, sourcePlayerId:number) {
  builder.addFieldInt32(1, sourcePlayerId, 0);
}

static addTargetPlayerId(builder:flatbuffers.Builder, targetPlayerId:number) {
  builder.addFieldInt32(2, targetPlayerId, 0);
}

static addKind(builder:flatbuffers.Builder, kind:StatusEffectKind) {
  builder.addFieldInt8(3, kind, StatusEffectKind.None);
}

static addPhase(builder:flatbuffers.Builder, phase:StatusEffectPhase) {
  builder.addFieldInt8(4, phase, StatusEffectPhase.Apply);
}

static addDurationTicks(builder:flatbuffers.Builder, durationTicks:number) {
  builder.addFieldInt32(5, durationTicks, 0);
}

static addMagnitude(builder:flatbuffers.Builder, magnitude:number) {
  builder.addFieldFloat32(6, magnitude, 0.0);
}

static addServerTick(builder:flatbuffers.Builder, serverTick:number) {
  builder.addFieldInt32(7, serverTick, 0);
}

static endStatusEffectEventPayload(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createStatusEffectEventPayload(builder:flatbuffers.Builder, effectId:number, sourcePlayerId:number, targetPlayerId:number, kind:StatusEffectKind, phase:StatusEffectPhase, durationTicks:number, magnitude:number, serverTick:number):flatbuffers.Offset {
  StatusEffectEventPayload.startStatusEffectEventPayload(builder);
  StatusEffectEventPayload.addEffectId(builder, effectId);
  StatusEffectEventPayload.addSourcePlayerId(builder, sourcePlayerId);
  StatusEffectEventPayload.addTargetPlayerId(builder, targetPlayerId);
  StatusEffectEventPayload.addKind(builder, kind);
  StatusEffectEventPayload.addPhase(builder, phase);
  StatusEffectEventPayload.addDurationTicks(builder, durationTicks);
  StatusEffectEventPayload.addMagnitude(builder, magnitude);
  StatusEffectEventPayload.addServerTick(builder, serverTick);
  return StatusEffectEventPayload.endStatusEffectEventPayload(builder);
}
}
