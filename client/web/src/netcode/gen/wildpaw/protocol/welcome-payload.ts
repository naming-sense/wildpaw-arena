// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

export class WelcomePayload {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):WelcomePayload {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsWelcomePayload(bb:flatbuffers.ByteBuffer, obj?:WelcomePayload):WelcomePayload {
  return (obj || new WelcomePayload()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsWelcomePayload(bb:flatbuffers.ByteBuffer, obj?:WelcomePayload):WelcomePayload {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new WelcomePayload()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

playerId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

serverTickRate():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

serverTick():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startWelcomePayload(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addPlayerId(builder:flatbuffers.Builder, playerId:number) {
  builder.addFieldInt32(0, playerId, 0);
}

static addServerTickRate(builder:flatbuffers.Builder, serverTickRate:number) {
  builder.addFieldInt32(1, serverTickRate, 0);
}

static addServerTick(builder:flatbuffers.Builder, serverTick:number) {
  builder.addFieldInt32(2, serverTick, 0);
}

static endWelcomePayload(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createWelcomePayload(builder:flatbuffers.Builder, playerId:number, serverTickRate:number, serverTick:number):flatbuffers.Offset {
  WelcomePayload.startWelcomePayload(builder);
  WelcomePayload.addPlayerId(builder, playerId);
  WelcomePayload.addServerTickRate(builder, serverTickRate);
  WelcomePayload.addServerTick(builder, serverTick);
  return WelcomePayload.endWelcomePayload(builder);
}
}
