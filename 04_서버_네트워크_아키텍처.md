# 04. 서버 · 네트워크 아키텍처 (Three.js + C++ Asio)

> 목적: "무렉에 가까운 체감"을 만들기 위한 **실행 가능한 구현 명세**
> 
> 범위: 실시간 전투(3v3/5v5), 매칭/입장/경기종료 파이프라인, 운영 관측/복구

---

## 1) 목표 품질 (체감 "무렉" 기준)

- **서버 틱**: 30Hz(기본), 경쟁 모드 60Hz 옵션
- **클라이언트 렌더**: 60FPS 목표
- **매치 내 지연 목표**
  - RTT p50 < 45ms
  - RTT p95 < 90ms
  - 지터 p95 < 15ms
- **서버 안정성**: 매치 서버 크래시율 < 0.1%/일
- **입장 품질**
  - 매치 할당 → 룸 접속 완료 p95 < 3초
  - 매치 시작 실패율 < 1%

핵심 원칙:
- 서버 authoritative (판정은 서버가 최종)
- 클라이언트는 예측/보간으로 체감 품질 보정
- 룸 단위 격리로 장애 반경 최소화

---

## 2) 전체 구성도

```text
[Web Client (Three.js)]
   ↕ WebSocket (WebTransport 가능시 우선)
[Edge Gateway]
   ├─ Auth Service
   ├─ Matchmaker
   ├─ Party/Presence
   └─ Realtime Room Allocator
              ↕
      [C++ Asio Game Room Servers]
              ↕
    [Redis (state cache) / Postgres (account, inventory)]
              ↕
    [Analytics Pipeline + Monitoring]
```

구성 원칙:
- Edge는 세션/라우팅 중심, 룸 시뮬레이션은 별도 워커
- DB 직접 접근은 gameplay critical path에서 최소화
- 로그/분석은 비동기 파이프라인으로 분리

---

## 3) 클라이언트(Three.js) 구조

### 3-1. 주요 모듈
- `render-core`: scene/camera/light/postprocess
- `gameplay-ecs`: 엔티티/컴포넌트/시스템
- `netcode`: 패킷 encode/decode, 예측/보간/복구
- `input`: 키/마우스 샘플링, 입력 버퍼링
- `ui`: HUD/로비/결과

### 3-2. 루프 분리
- 렌더 루프: `requestAnimationFrame` (가변)
- 시뮬 루프: 고정 스텝(기본 33.3ms)
- 네트워크 루프: 수신 시점 독립 처리

### 3-3. 지연 보정 4종
1. **Client-side Prediction**: 내 이동/사격 선반영
2. **Server Reconciliation**: authoritative 상태로 오차 수정
3. **Interpolation Buffer**: 타 유저 80~120ms 버퍼 보간
4. **Lag Compensation**: 히트 판정 시 서버 리와인드

### 3-4. 필수 구현 디테일
- 입력 프레임에 `inputSeq`(증가값) 포함
- 클라는 최근 N프레임 입력(권장 120프레임) 링버퍼 저장
- authoritative snapshot의 `lastProcessedInputSeq` 기준으로 재적용
- 보간 버퍼는 네트워크 품질에 따라 동적(80~140ms) 조절

---

## 4) C++ Asio 실시간 서버 설계

### 4-1. 기술 스택
- C++20
- standalone Asio
- TLS/HTTP 업그레이드(초기 핸드셰이크)
- 직렬화: FlatBuffers 또는 커스텀 바이너리(VarInt + bitpack)

### 4-2. 프로세스 모델
- `gateway process`: 인증/라우팅
- `room worker process`: 게임 시뮬레이션
- 룸 단위 격리 (한 룸 장애가 전체 장애로 전파되지 않도록)

### 4-3. 스레드 모델 (권장)
- 네트워크 I/O: `io_context` 스레드풀 (코어 기준 N)
- 룸 시뮬: 룸별 직렬 실행(`strand` 또는 룸 큐)
- 외부 I/O(DB/로그): 별도 비동기 job queue

### 4-4. 룸 틱 처리 순서 (authoritative)
1) 입력 수집
2) 이동/충돌
3) 사격/투사체
4) 스킬/버프
5) 판정/점수
6) 델타 스냅샷 생성

- 룸 최대 인원: 10명(5v5)
- 목표 틱 budget(30Hz): **한 틱 33.3ms 중 시뮬 p95 < 8ms**

---

## 5) 네트워크 프로토콜

### 5-1. 채널 분리
- 신뢰 채널: 매치 시작/종료, 인벤토리, 확정 이벤트
- 비신뢰 저지연 채널: 위치, 에임, 투사체 상태

### 5-2. 패킷 최적화
- 델타 압축(변경 필드만)
- Interest Management(근접/가시 우선)
- 스냅샷 주기 가변(거리/가시성 기반 10~30Hz)
- 압축 전송(zstd/lightweight)

### 5-3. 대역폭 목표 (1인당)
- 수신: 10~25KB/s
- 송신: 8~20KB/s
- 10인 룸 기준 서버 egress 최적화 필수

### 5-4. 메시지 타입 (MVP)
- `C2S_HELLO`
- `C2S_INPUT`
- `C2S_PING`
- `C2S_ACK`
- `S2C_WELCOME`
- `S2C_MATCH_START`
- `S2C_SNAPSHOT_BASE`
- `S2C_SNAPSHOT_DELTA`
- `S2C_EVENT` (킬/득점/오브젝트)
- `S2C_MATCH_END`

권장 헤더 필드:
- `msgType(u16)`
- `roomId(u32)`
- `clientSeq(u32)`
- `ack(u32)`
- `ackBits(u32)`
- `serverTick(u32)`

---

## 6) 대규모 접속 대응 전략

### 6-1. 수평 확장
- 룸 서버는 stateless에 가까운 세션 구조
- Matchmaker가 지역/핑/MMR 기반 배치
- 오토스케일 입력 지표: 큐 길이, 활성 룸 수, CPU p95

### 6-2. 샤딩/리전
- KR/JP/SG/US 리전 분리
- 파티 평균 핑 상한(예: 120ms) 초과 시 경고 및 품질 안내

### 6-3. 장애 격리
- 룸 크래시 시 해당 매치만 영향
- 게이트웨이 다중화 + 헬스체크 + 자동 재시작
- 룸 할당 실패 시 fallback 리전/서버 재시도

---

## 7) 운영/관측 (Observability)

수집 메트릭:
- 틱 처리시간(평균/p95/p99)
- RTT/지터/패킷 드랍률
- 룸당 CPU/Mem/네트워크
- 매치 생성 성공률, 입장 성공률

로그:
- 매치 이벤트 구조화(JSON)
- `matchId`, `roomId`, `playerId`, `tick`, `eventType` 필수

트레이싱:
- 로그인 → 매칭 → 입장 → 경기 종료 end-to-end 추적

알람 기준:
- p95 RTT 급등
- 특정 리전 드랍률 급증
- 매치 생성 실패율 임계치 초과

---

## 8) 보안/치트 대응

- 서버 authoritative 판정 (클라 신뢰 금지)
- 입력 빈도/패턴 검증(매크로 탐지)
- 비정상 이동/발사 레이트 제한
- 신고 대응용 리플레이 이벤트 로그
- TLS + 토큰 기반 세션 검증
- 룸 입장 시 JWT 검증 + 만료시간 강제

---

## 9) 구현 마일스톤 (기술)

1. **M1 (4주)**
   - WebSocket 룸 입장
   - 이동 동기화 + 기본 사격
   - 최소 모니터링(RTT/틱 시간)

2. **M2 (4주)**
   - 예측/복구/보간
   - 히트 판정 리와인드 안정화
   - 패킷 델타 압축

3. **M3 (5주)**
   - 스킬/오브젝트/모드 룰
   - 매치메이커 + 리전 라우팅
   - 룸 장애 자동 대체(failover)

4. **M4 (4주)**
   - 부하/혼잡/패킷 손실 시나리오 테스트
   - 운영 지표/알람 완성
   - 소프트런칭 준비

---

## 10) 부하 테스트 기준 (출시 전)

- CCU: 5k → 10k → 30k 단계
- 시나리오:
  - 큐 폭주
  - 룸 서버 재시작
  - 리전 단절
  - 패킷 손실 5~10%

합격 기준:
- 매치 시작 실패율 < 1%
- 강제 종료율 < 0.5%
- p95 서버 틱 지연 < 8ms (30Hz)

---

## 11) 구현용 서비스 계약 (API/이벤트)

### 11-1. Gateway HTTP (초기)
- `POST /v1/auth/guest`
  - req: 디바이스/플랫폼 정보
  - res: access token, playerId
- `POST /v1/matchmaking/enqueue`
  - req: mode, partyId(optional), regionPreference
  - res: ticketId
- `POST /v1/matchmaking/dequeue`
  - req: ticketId

### 11-2. Gateway → Client 실시간 이벤트
- `match.found`: `matchId`, `roomEndpoint`, `roomToken`
- `match.cancelled`: reason
- `party.updated`: 파티원 상태 변경

### 11-3. Room 접속 핸드셰이크
1) Client `C2S_HELLO(roomToken, clientVersion)`
2) Server `S2C_WELCOME(playerSlot, serverTickRate, snapshotBase)`
3) Client부터 `C2S_INPUT` 스트림 시작

---

## 12) 룸 서버 내부 설계 (실행 관점)

### 12-1. 권장 디렉토리 구조 (실제 개발 레포 기준)
```text
server/
  gateway/
  matchmaker/
  room/
    src/
      net/
      sim/
      ecs/
      anti_cheat/
      snapshot/
    include/
shared/
  protocol/
  telemetry/
client/
  web/
    src/netcode/
```

### 12-2. 핵심 클래스
- `RoomSession`: 클라이언트 연결/전송/ack 상태
- `RoomSimulation`: authoritative world + tick 실행
- `InputBuffer`: 플레이어별 입력 프레임 큐
- `SnapshotBuilder`: baseline/delta 스냅샷 생성
- `InterestManager`: 엔티티 가시/거리 필터링

### 12-3. 룸 틱 의사코드
```cpp
void RoomSimulation::tick(uint32_t tickId) {
  collectInputs(tickId);               // timeout 입력은 마지막 입력 재사용
  applyMovement();                     // 물리/충돌
  processProjectiles();                // 발사체/충돌
  processSkillsAndBuffs();             // 스킬/상태이상
  resolveDamageAndScore();             // 체력/킬/점수
  auto events = drainAuthoritativeEvents();
  auto snapshots = snapshotBuilder.buildDelta(world, tickId);
  broadcaster.send(events, snapshots);
  antiCheat.scanFrame(world, tickId);
}
```

### 12-4. 입력 유실 처리
- `inputSeq` 누락 허용 범위: 최대 2~3프레임
- 초과 누락 시:
  - 마지막 입력 유지(짧게)
  - 이후 감쇠(가속/이동량 제한)
  - 장기 유실은 idle 처리

---

## 13) 클라이언트 Netcode 구현 지침

### 13-1. 로컬 플레이어
- 입력 즉시 로컬 시뮬 적용
- 전송 시 `inputSeq`와 `localTick` 포함
- 서버 snapshot 수신 시 `lastProcessedInputSeq`까지 롤백 후 재적용

### 13-2. 원격 플레이어
- snapshot 2개 이상 확보 시 보간
- 목표 보간 지연값: 100ms 기본
- 지터 증가 시 버퍼 자동 증가(최대 140ms)

### 13-3. 워프 방지
- 위치 오차가 임계값(예: 1.5m) 이하이면 스무딩
- 임계값 초과 시 단계적 보정(3~5프레임)
- 텔레포트 이벤트는 예외 처리(즉시 위치 반영)

---

## 14) 데이터 저장 원칙

### 14-1. Redis
- 매치 티켓/큐 상태
- 파티 presence
- 룸 할당 임시 상태(TTL 필수)

### 14-2. Postgres
- 계정/인벤토리/결과 기록
- 경기 종료 후 요약 결과만 저장(전투 프레임 raw 저장 금지)

### 14-3. 이벤트 파이프라인
- 실시간 이벤트는 큐(Kafka/NATS 등)로 비동기 적재
- 분석 지표 집계는 게임 서버와 분리

---

## 15) 운영 Runbook (요약)

### 15-1. p95 RTT 급등
1) 리전별 RTT/지터 분리 확인
2) 특정 ASN/ISP 편향 여부 확인
3) 해당 리전 룸 과밀 시 scale-out
4) 필요 시 신규 매치의 타 리전 우회 허용

### 15-2. 매치 시작 실패율 상승
1) Matchmaker queue backlog 확인
2) Room allocator capacity 확인
3) 실패 코드별 상위 원인 추적
4) 임시로 동시 매치 생성량 제한(서킷 브레이커)

### 15-3. 룸 서버 크래시 반복
1) 크래시 룸 코어덤프 수집
2) 해당 빌드 롤백
3) 재현 시나리오 부하테스트 재실행

---

## 16) MVP 완료 정의 (Definition of Done)

- 3v3 기준 10,000판 내부 시뮬레이션에서:
  - 강제 종료율 < 0.5%
  - 매치 시작 실패율 < 1%
  - p95 RTT < 90ms
  - p95 틱 지연 < 8ms
- 클라이언트 체감 지표(유저 테스트):
  - "이동/사격이 끊긴다" 응답 비율 < 10%
- 운영 준비:
  - 대시보드/알람/로그/트레이스 모두 연결 완료

---

## 17) 바로 실행할 다음 작업 (2주)

1. Room 서버 MVP(이동/사격/판정) 구현
2. 클라 입력 예측 + 서버 복구 루프 연결
3. Snapshot delta + interest management 1차 적용
4. RTT/지터/틱시간 대시보드 구축
5. 50~100 동접 내부 부하테스트 자동화

> 한 줄 요약: **"판정은 서버, 체감은 클라 보정"**을 끝까지 일관되게 밀어야 웹 PvP 품질이 나온다.
