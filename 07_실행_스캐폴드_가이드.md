# 07. 실행 스캐폴드 가이드 (M1 착수본)

이번 단계는 "문서"에서 "실행 가능한 서버/클라 뼈대"로 넘어가기 위한 구현 착수본입니다.

## 1) 추가된 구조

```text
server/
  CMakeLists.txt
  room/
    CMakeLists.txt
    include/room/
      input_buffer.hpp
      room_simulation.hpp
      room_session.hpp
      snapshot_builder.hpp
      interest_manager.hpp
      wire_json.hpp
      wire_binary.hpp
      wire_flatbuffers.hpp
    src/
      main.cpp
      input_buffer.cpp
      room_simulation.cpp
      room_session.cpp
      snapshot_builder.cpp
      interest_manager.cpp
      wire_json.cpp
      wire_binary.cpp
      wire_flatbuffers.cpp
  gateway/README.md
  matchmaker/README.md

shared/
  protocol/
    fbs/
      wildpaw_protocol.fbs
    generated/
      cpp/
        wildpaw_protocol_generated.h
    message_types.hpp
    packet_header.hpp
    README.md

client/
  web/
    package.json
    src/netcode/
      gen/
        wildpaw/protocol/*.ts
      types.ts
      ringBuffer.ts
      prediction.ts
      interpolation.ts
      netClient.ts
      index.ts
```

## 2) 현재 구현 범위
- Room 서버
  - WebSocket(Boost.Beast) **binary-only** 수신/송신
  - FlatBuffers Envelope decode (`HELLO/INPUT/PING`) → 시뮬 입력 연결
  - 30Hz authoritative tick
  - FlatBuffers SnapshotPayload(`Base/Delta`) 전송
  - 거리 기반 interest filter 후 변경 엔티티 전송
  - `seq/ack/ackBits` 메타 트래킹(`RoomSession`) 연결
- Client netcode
  - 입력 예측/복구 기본 함수
  - snapshot 보간 버퍼
  - FlatBuffers 기반 WebSocket 클라이언트 래퍼
  - `seq/ack/ackBits` 자동 송신/수신 추적
  - `WELCOME/SNAPSHOT/EVENT` binary payload decode
- 프로토콜
  - `shared/protocol/fbs/wildpaw_protocol.fbs` 기준 단일 스키마
  - C++/TS 코드젠 산출물 포함

## 3) 실행 방법
```bash
cd server
cmake -S . -B build
cmake --build build -j
./build/room/wildpaw-room 7001
```

## 4) 현재 TODO
- 신뢰/비신뢰 채널 분리(transport 레벨)
- 히트 판정 + lag compensation(리와인드)
- flatc 코드젠 자동화(CMake/CI)
- gateway/matchmaker 실코드
- 부하 테스트용 bot client

## 5) 다음 우선순위 (권장)
1. 클라이언트 Three.js ECS와 netcode 모듈 직접 통합
2. 10인 로컬 봇 시뮬레이터 작성
3. p95 tick/RTT/드랍률 메트릭 노출
4. 룸 서버 retransmit/유실 복구 큐 추가
5. flatc 코드젠 자동화 + 프로토콜 버전 호환 전략 수립
